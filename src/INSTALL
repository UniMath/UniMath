# -*- makefile-gmake -*-

Quick start, in emacs, visiting this file:

  M-x compile

Slow start:

  Initial setup, one time only:

    coq_makefile -f Make-include.in

  To build (check the proofs):

    make

  To make the documentation:

    make html

To install:

  make install

# Local Variables:
# compile-command: "if [ ! -f Make-include.mk ] ; then coq_makefile -f Make-include.in ; fi && make all html "
# End:
