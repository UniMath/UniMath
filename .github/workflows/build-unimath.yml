name: CI-Build-UniMath

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains two jobs, called "build-ubuntu" and "build-macos"

  sanity-checks:
    name: Sanity-Checks-Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install coq
          type coqc
          coqc --version
      - name: Run sanity checks
        run: |
          cd $GITHUB_WORKSPACE
          time make sanity-checks

  build-Unimath-ubuntu:
    name: Build-UniMath-Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install coq
          type coqc
          coqc --version
      - name: Build UniMath
        run: |
          cd $GITHUB_WORKSPACE
          time make -j2 TIMECMD=time

  build-coq-and-Unimath-ubuntu:
    name: Build-coq-and-UniMath-Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install build-essential git ocaml ocaml-nox ocaml-native-compilers camlp5 libgtk2.0 libgtksourceview2.0 liblablgtk-extras-ocaml-dev ocaml-findlib libnum-ocaml-dev emacs
      - name: Build UniMath
        run: |
          cd $GITHUB_WORKSPACE
          time make -j2 TIMECMD=time BUILD_COQ=yes

  build-macos:
    name: Build-UniMath-on-MacOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install build dependencies
        run: |
           brew install coq
           : --- show some configuration information ---
           brew config --verbose
           type coqc
           coqc --version
      - name: Build UniMath
        run: |
          cd $GITHUB_WORKSPACE
          time make -j2 TIMECMD=time

#### ocaml 4.12 is currently too modern to compile this version of coq, and that's what github provides.
#  build-macos-with-coq:
#    name: Build-coq-and-UniMath-on-MacOS
#    runs-on: macos-latest
#    steps:
#      - uses: actions/checkout@v2
#      - name: Install build dependencies
#        run: |
#           brew install bash objective-caml ocaml-num ocaml-findlib camlp5
#           : --- show some configuration information ---
#           brew config --verbose
#      - name: Build UniMath
#        run: |
#          cd $GITHUB_WORKSPACE
#          time make -j2 TIMECMD=time BUILD_COQ=yes
#